steps = FindVersion, ChangeVersion, CheckChangeLog, DistTest, Commit, Tag, MakeDist, UploadCPAN, AddToSVNDir
AddToSVNDir.dir = /home/lj/cvs/web-danga/dist/Gearman/
svn.tagpattern = Gearman-Client-Async-%v

# CheckChangeLog.files = ChangeLog, MyProj.CHANGES
